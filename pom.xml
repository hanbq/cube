<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.1</version>
    </parent>

    <groupId>com.cube</groupId>
    <artifactId>cube</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <cube.version>0.0.1-SNAPSHOT</cube.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>33.5.0-jre</version>
            </dependency>

            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>0.11.5</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>0.11.5</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>0.11.5</version>
                <scope>runtime</scope>
            </dependency>

            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>3.8.1</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>com.cube</groupId>
                <artifactId>cube-common</artifactId>
                <version>${cube.version}</version>
            </dependency>

            <dependency>
                <groupId>com.cube</groupId>
                <artifactId>cube-api</artifactId>
                <version>${cube.version}</version>
            </dependency>

            <dependency>
                <groupId>com.cube</groupId>
                <artifactId>cube-system-api</artifactId>
                <version>${cube.version}</version>
            </dependency>

            <dependency>
                <groupId>com.cube</groupId>
                <artifactId>cube-system-core</artifactId>
                <version>${cube.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <modules>
        <module>cube-api</module>
        <module>cube-server</module>
        <module>cube-workflow</module>
        <module>cube-system</module>
        <module>cube-common</module>
    </modules>

</project>