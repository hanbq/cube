import { Box, Typography } from '@mui/material';

export default function WorkflowDefinition() {
  return (
    <Box>
      <Typography variant="h4">流程定义</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        流程定义页面开发中...
      </Typography>
    </Box>
  );
}
