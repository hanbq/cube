import { Box, Typography } from '@mui/material';

export default function WorkflowInstance() {
  return (
    <Box>
      <Typography variant="h4">流程实例</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        流程实例页面开发中...
      </Typography>
    </Box>
  );
}
